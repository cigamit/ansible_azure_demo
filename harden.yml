---
- name: Harden Server
  hosts: "{{ vm_name }}"
  vars_files:
    - 'vars/harden.yml'
  tasks:
    - debug:
        var: harden-role

    - debug:
        msg: "{{ harden-role[vm] }}"


    - include_role:
        name: "{{ harden-role[vm] }}"
