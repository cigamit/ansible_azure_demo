common:
  CentOS:
    packages:
      - mlocate
      - wget
      - telnet
  Windows:
    packages:
      - 7zip
      - vscode

apps:
  CentOS:
    WebServer: centos_nginx
    Database:  centos_mariadb
  Windows:
    WebServer: windows_iis
    Database: windows_sql2019exp
    DomainController: windows_dc